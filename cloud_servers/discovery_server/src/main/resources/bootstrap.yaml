---
# Properties active for ALL profiles:
spring:
  application:
    name: discovery-server


# Properties related to Config server - currently not in use
---
spring:
  cloud:
    config:
      uri: http://localhost:8888
