---
# Properties active for ALL profiles:
spring:
  application:
    name: db_client_demo


# Properties related to Config server
# Properties activated if "demo" is the active profile:
---
spring:
  profiles:
    active: demo
  cloud:
    config:
      uri: http://localhost:8888
